# version_control
First Bad Version (Java)
This Java program simulates the LeetCode "First Bad Version" problem. The goal is to identify the first version in a sequence that causes all subsequent versions to be bad.

The solution uses an efficient binary search approach to minimize the number of calls to the isBadVersion() API. A mock VersionControl class is included to test the logic locally outside of the LeetCode environment.
